# 日记API测试界面使用指南

这是一个简单的HTML测试界面，用于测试日记API的所有功能。

## 功能概述

该测试界面包含以下API测试功能：

1. **创建日记** - 测试创建新日记的功能
2. **获取日记详情** - 测试根据ID获取特定日记的功能
3. **更新日记** - 测试更新现有日记的功能
4. **删除日记** - 测试删除日记的功能
5. **获取用户日记列表** - 测试分页获取用户所有日记的功能
6. **获取用户最新日记** - 测试获取用户最新日记的功能
7. **获取日记预览列表** - 测试获取日记预览（不包含完整内容）的功能
8. **搜索日记** - 测试根据关键词搜索用户日记的功能
9. **获取用户日记总数** - 测试获取用户日记总数的功能
10. **获取情绪标签统计** - 测试获取用户情绪标签统计的功能

## 使用方法

1. 确保后端服务正在运行，默认地址为 `http://localhost:8000`
2. 在浏览器中打开 `diary_test_frontend.html` 文件
3. 在配置部分设置正确的API基础URL和默认用户ID
4. 按顺序测试各个API功能：
   - 首先创建一个日记
   - 然后获取日记详情验证创建成功
   - 接着更新日记内容
   - 最后可以删除日记
   - 其他功能可以独立测试

## 注意事项

- 确保后端服务正在运行，否则所有API调用都会失败
- 创建日记后，系统会自动填充日记ID到其他相关表单中
- 元数据字段需要输入有效的JSON格式
- 标签可以通过输入框添加，按回车键确认
- 所有响应都会显示在对应的功能区域下方，成功响应显示为绿色，错误响应显示为红色

## 技术实现

- 使用原生HTML、CSS和JavaScript实现，不依赖任何框架
- 使用Fetch API进行HTTP请求
- 响应式设计，适配不同屏幕尺寸
- 错误处理和用户友好的提示信息

## 自定义配置

- 可以通过修改API基础URL来连接到不同的后端服务
- 可以修改默认用户ID来测试不同用户的数据
- 可以调整限制数量和偏移量来测试分页功能